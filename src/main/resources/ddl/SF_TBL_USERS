CREATE TABLE wjlee.dbo.SF_TBL_USERS(
  USER_ID BIGINT IDENTITY(1,1) PRIMARY KEY,
  USERNAME NVARCHAR(64) UNIQUE NOT NULL,
  PASSWORD_HASH NVARCHAR(200) NOT NULL,
  ROLES NVARCHAR(64) NOT NULL,   -- "ROLE_USER,ROLE_ADMIN"
  ACTIVE BIT NOT NULL DEFAULT 1,
  REG_DATE DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP
);

/*
INSERT INTO wjlee.dbo.SF_TBL_USERS(username, password_hash, roles, active)
VALUES (
  'admin',
  '$2a$10$0n0bBfcm6Dj2CgyV5XPQTOjp1fQbkVCHeQkSVyM4bGIgdpit5UJ1G',
  'ROLE_ADMIN',
  1
);
*/